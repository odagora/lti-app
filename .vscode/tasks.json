{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "install dependencies",
      "type": "shell",
      "command": "npm install && pip install -r requirements.txt",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}